import type { NextPage } from "next";

import { PageTitle } from "../../components/PageTitle";

const history: NextPage = () => {
  return <PageTitle title={"History"} sub={"AISM 연혁 소개"} />;
};

export default history;
