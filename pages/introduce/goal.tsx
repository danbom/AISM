import type { NextPage } from "next";

import { PageTitle } from "../../components/PageTitle";

const goal: NextPage = () => {
  return <PageTitle title={"Goal"} sub={"추구하는 목표 소개"} />;
};

export default goal;
