import type { NextPage } from "next";

import { PageTitle } from "../../components/PageTitle";

const props: NextPage = () => {
  return <PageTitle title={"Props"} sub={"특장점 소개"} />;
};

export default props;
